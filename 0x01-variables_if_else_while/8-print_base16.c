#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - entry
 *Return: =0
 */
int main(void)
{
	for (int i = '0'; i <= '9'; i++)
		putchar(i);
	for (int i = 97; i <= 102; i++)
		putchar(i);
	purchar('\n');
	return (0);
}
